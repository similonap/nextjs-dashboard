import { Notable, Inter, Lusitana, Roboto } from "next/font/google";

export const notable = Notable({subsets: ["latin"], weight: "400"});
export const inter = Inter({subsets: ["latin"]});
export const lusitana = Lusitana({weight: "700", subsets: ["latin"]});