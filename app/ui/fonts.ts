import { Notable, Inter, Lusitana, Roboto } from "next/font/google";

export const notable = Notable({weight: "400"});
export const inter = Inter({subsets: ["latin"]});
export const lusitana = Lusitana({weight: "700"});